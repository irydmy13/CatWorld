<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CatWorld.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Settings">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">

            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                <Label Text="Night schedule"/>
                <Switch Grid.Column="1" IsToggled="{Binding NightScheduleEnabled}"/>
            </Grid>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="16" IsEnabled="{Binding NightScheduleEnabled}">
                <VerticalStackLayout>
                    <Label Text="Start"/>
                    <TimePicker Time="{Binding NightStart}"/>
                </VerticalStackLayout>
                <VerticalStackLayout>
                    <Label Text="End"/>
                    <TimePicker Time="{Binding NightEnd}"/>
                </VerticalStackLayout>
            </Grid>

            <Button Text="Save" Command="{Binding SaveCommand}"/>

            <Label FontSize="12" Opacity="0.7"
                   Text="Tip: If Start > End, schedule spans midnight (e.g., 22:00–07:00)." />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
